# Brahmastra Backend — Environment Variables
# Copy this to .env, fill in values, NEVER commit .env to git!

# ── Server ─────────────────────────────────────
SERVER_HOST=http://15.206.82.159
CORS_ORIGINS=http://15.206.82.159,http://15.206.82.159:8080,http://localhost:8080

# ── Security ────────────────────────────────────
# Generate: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=REPLACE_WITH_STRONG_RANDOM_KEY

# ── Database ────────────────────────────────────
# Get password: cd infrastructure && terraform output -raw db_password
DATABASE_URL=postgresql://brahmastra_admin:REPLACE_PASSWORD@brahmastra-db.ctuc8ygwmfbb.ap-south-1.rds.amazonaws.com:5432/brahmastra_db

# ── AWS SES Email (optional but recommended) ────
# Must be a verified sender in AWS SES
SES_FROM_EMAIL=noreply@yourdomain.com
AWS_REGION=ap-south-1
# If not using EC2 instance role, set these:
# AWS_ACCESS_KEY_ID=your_key
# AWS_SECRET_ACCESS_KEY=your_secret

# ── Admin Alerts ────────────────────────────────
# Email to receive security alerts (future use)
ADMIN_EMAIL=admin@yourdomain.com
